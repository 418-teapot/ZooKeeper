/**
 * Context command hook barrel export.
 *
 * Thin re-export — all logic lives in `src/core/context/dcp-command.ts`.
 *
 * @module
 */

export {
  DCP_COMMAND_HANDLED,
  handleDcpCommand,
} from "../../core/context/dcp-command.js";
