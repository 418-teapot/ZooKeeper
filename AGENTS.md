# ZooKeeper 代理开发指南

## 目标

本仓库维护一个同时适配 OpenCode 和 pi 的编排器插件，并包含安装脚本、测试程序和 Rust 工具。

## 不可违反的约束

### 配置与安全

- `config.toml` 是所有可调运行时行为的唯一配置来源。不要在代码中添加默认值、备用值或重复配置。
- 配置缺失、解析失败或依赖不可用时，相关功能必须跳过或关闭；不得带着猜测继续运行。
- 核心逻辑保持框架无关；OpenCode、pi 及其他宿主的类型和事件适配留在适配层。

### 代码与文本

- 注释使用英文，面向用户的输出使用中文。
- 注释解释代码职责或非显然原因，不得记录施工过程、层级编号、外部叙事、黑话等。
- Python 使用 Ruff 和 Google 风格 docstring。
- TypeScript 使用 Biome、2 空格、双引号和 JSDoc；行宽 80 列。
- Rust 使用 rustfmt 和 clippy；行宽 80 列，禁止 `unsafe`，禁止用 `#[allow]` 或 `#[expect]` 压制 lint。
- 优先保持改动小而局部；不要为了整理代码顺手扩大修改范围。

### Git 与工作区

- 先检查工作区状态，不得覆盖或回滚用户已有的修改。
- 未经明确要求，不得执行 `git add`、`git commit`、`git push` 或其他提交操作。
- 用户要求提交时，加载 git-commit skill，并按该流程执行。
- 生成的用户配置、环境变量和构建产物不应写入仓库；以 `.gitignore` 和现有脚本为准。

## 工作方式

1. 先根据当前请求判断是讨论、探索、诊断还是实现；不要把上一轮的意图自动带入。
2. 实现前先搜索相关代码、测试、配置和脚本；能从仓库得到的事实不要询问用户。
3. 发现需求存在会显著改变工作量的多种解释时，先列出选项、推荐项和影响，等待用户决定。
4. 修改前确认根因，避免用猜测覆盖症状；涉及结构变化时优先复用现有抽象，不绕过注册表、配置解析或宿主适配边界。
5. 修改后检查差异，并运行与变更相关的验证；完成代码修改时必须运行项目检查和测试命令。
6. 测试失败时区分本次改动引入的问题、已有失败和环境缺失；没有证据不得报告完成。

## 标准命令

在仓库根目录运行：

```sh
uv run python install.py  # 读取 config.toml 和 .env，生成宿主配置

# 这些命令的输出会很长，需要精简终端输出，可以保存日志到文件中
./check.sh                # 自动格式化并执行 Python/TypeScript/Rust 严格检查
./test.sh                 # 执行 Python、Rust、TypeScript 测试和覆盖率检查
./build.sh                # Rust 工具和 zweb addon 的 release 构建
```

如果修改 Rust 工具，至少运行 `./build.sh`；如果只修改文档或配置说明，仍需检查文档差异，并在配置或脚本语义受影响时运行相应验证。
